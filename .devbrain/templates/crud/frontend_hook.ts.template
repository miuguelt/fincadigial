import { useResource } from '@/shared/hooks/useResource';
import { {{module_name}}Service } from '@/entities/{{module_name}}/api/{{module_name}}.service';
import type { {{ModelName}} } from '@/entities/{{module_name}}/model/types';

export interface Use{{ModelName}}Result {
  data: {{ModelName}}[];
  loading: boolean;
  error: string | null;
  refetch: (params?: Record<string, any>) => Promise<{{ModelName}}[]>;
  createItem: (payload: Partial<{{ModelName}}>) => Promise<{{ModelName}} | null>;
  updateItem: (id: number | string, payload: Partial<{{ModelName}}>) => Promise<{{ModelName}} | null>;
  deleteItem: (id: number | string) => Promise<boolean>;
}

export function use{{ModelName}}(): Use{{ModelName}}Result {
  const resource = useResource<{{ModelName}}, any>({{module_name}}Service as any, { autoFetch: true });
  return {
    data: resource.data as unknown as {{ModelName}}[],
    loading: (resource as any).loading,
    error: (resource as any).error,
    refetch: resource.refetch as unknown as (params?: Record<string, any>) => Promise<{{ModelName}}[]>,
    createItem: resource.createItem as unknown as (payload: Partial<{{ModelName}}>) => Promise<{{ModelName}} | null>,
    updateItem: resource.updateItem as unknown as (id: number | string, payload: Partial<{{ModelName}}>) => Promise<{{ModelName}} | null>,
    deleteItem: resource.deleteItem,
  };
}
