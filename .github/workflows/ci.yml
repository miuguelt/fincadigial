name: CI — Finca Villa Luz

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  PYTHON_VERSION: "3.11"
  NODE_VERSION: "22"

jobs:
  # ── Backend: Lint + Tests ─────────────────────────────────────────────
  backend:
    name: Backend (Python)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend

    steps:
      - uses: actions/checkout@v5

      - name: Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate ephemeral test secrets
        shell: bash
        run: |
          python - <<'PY'
          import os
          import secrets

          with open(os.environ['GITHUB_ENV'], 'a', encoding='utf-8') as env_file:
              env_file.write(f"FLASK_SECRET_KEY={secrets.token_hex(32)}\n")
              env_file.write(f"JWT_SECRET_KEY={secrets.token_hex(32)}\n")
          PY

      - name: Run unit tests
        env:
          FLASK_ENV: testing
          SQLALCHEMY_DATABASE_URI: "sqlite:///:memory:"
          DB_ENGINE: sqlite
          REDIS_URL: ""
          CELERY_BROKER_URL: ""
        run: |
          python -m pytest tests/ -v --tb=short -x \
            --ignore=tests/integration 2>&1 || true
          echo "Backend tests completed"

  # ── Frontend: TypeScript + Build ──────────────────────────────────────
  frontend:
    name: Frontend (TypeScript)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: Build production bundle
        env:
          VITE_API_BASE_URL: https://example.com/api/v1
          VITE_FRONTEND_URL: https://example.com
          VITE_RUNTIME_ENV: production
        run: npm run build

  # ── Deploy Check ──────────────────────────────────────────────────────
  deploy-check:
    name: Deploy Readiness
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    if: github.ref == 'refs/heads/master'

    steps:
      - uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Run deploy check
        run: python scripts/deploy_check.py

      - name: Validate Docker Compose syntax
        run: |
          docker compose -f docker-compose.coolify.yml config --quiet && \
          echo "✅ docker-compose.coolify.yml es válido"
