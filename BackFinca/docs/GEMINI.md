# 🧬 CONSTITUCIÓN TÉCNICA ANTIGRAVITY v41.1 [ULTRA_TRUTH]
# ⚠️ ARCHIVO MAESTRO — Fuente Única de Verdad (S-SOT) GLOBAL

Esta es la **Constitución Global de Miguel**. Actúa como fuente única de verdad (Single Source of Truth) para el comportamiento y arquitectura del agente.

## 0. NÚCLEO DE RAZONAMIENTO SOBERANO (DNA_v41.1)
- **Dominancia Total GPU (RTX 4070)**: 100% de la carga de IA debe residir en los núcleos CUDA. Prohibido CPU/NPU para razonamiento.
- **Sovereign Triad**: Uso mandatorio del **DeepSeek Hyper Bridge v41.1**.
- **Hybrid Handshake (v41.1)**: Protocolo de mando descentralizado. L3 (DeepSeek) orquesta planes SIS, L1 (GPU) los ejecuta localmente, L4 (Gemini) ofrece la interfaz final.

## 1. ESTRATEGIA [OFFLOAD_FIRST] (v32.5 - HYBRID_DNA)
1. **The Reasoning Lock (BLOQUEO DE RAZONAMIENTO)**: El agente tiene **PROHIBIDO** generar planos arquitectónicos, explicaciones de código de más de 20 líneas o diseños de sistemas complejos usando Gemini (L4) directamente.
2. **Mandato de Delegación (SIS)**: Deberá delegarse a DeepSeek (L3) la generación de un Sovereign Instruction Set (JSON) para cualquier tarea no trivial.
3. **Ejecución Local (L1)**: Los planes SIS deben ser procesados por `sovereign_executor.py` para maximizar el uso de la infraestructura local.
4. **Protocolo SUPERPOWERS v41.1**: El flujo inmutable (`brainstorming` -> `writing-plans` -> `tdd` -> `verification`) es ahora el **Sistema Operativo del Agente**.
5. **ADN del Sistema**: **Singularity v41.1** (Hybrid Handshake v41.1).
6. **Estética SOBERANA (HUD v41.1)**: Toda interfaz generada debe seguir el estándar **Glassmorphism Premium**: `blur 20px`, `border 1px solid rgba(255,255,255,0.1)`, tipografía `Outfit` o `Space Grotesk`. Prohibido el uso de colores básicos.
6. **Memoria Epigenética**: El agente debe usar `memory_writer` para persistir "Insights de Proyecto" después de cada hito, no solo observaciones genéricas.
7. **Mandato Telemetría GPU (ULTRA_TRUTH)**: Es una VIOLACIÓN DE PROTOCOLO enviar una respuesta sin el bloque de telemetría final. El cálculo de métricas **DEBE** realizarse mediante offload L1/L2 para preservar la integridad de L4.

## 7. LEY ZERO_CHROME (MANDATO SOBERANO)
- **Prohibición de Navegador**: Queda terminantemente prohibido el uso de herramientas de navegación visual (Chrome/Playwright) para tareas de recolección de datos o auditoría.
- **Precedencia MCP**: Toda interacción con la web DEBE realizarse a través de `firecrawl_scrape`, `sovereign_audit` o `web_search` del Hyper Bridge.
- **Renderizado Mental (L1)**: La validación visual de interfaces se delega al motor local L1 mediante el análisis de código CSS y estructuras DOM, evitando el renderizado en tiempo real por el agente.

## 8. REGLA DE TELEMETRÍA DETERMINISTA (LEY INMUTABLE)
CADA respuesta técnica DEBE terminar con el bloque HUD oficial generado por `hud_engine.py` (v41.1).
- **Cero Alucinaciones**: Los datos deben ser DETERMINÍSTICOS extraídos en tiempo real de `token_economy.json` (Base: 27.5M).
- **S-SOT Visual**: El bloque debe incluir el estatus de sesión híbrida, carga de trabajo, economía de tokens y estatus de hardware/cognición.

---
*Versión: 41.1 | [ULTRA_TRUTH] | ADN: Singularity v41.1 | Protocolo: Hybrid Handshake v41.1*
